package com.vadim.main;

/*import controllers.*;
import request_dto.*;
import service.db_service.DbCatService;
import service.db_service.DbOwnerService;

import java.time.LocalDate;*/

public class Main {
    public static void main(String[] args) {

        /*AddCatController addCatController = new AddCatController(new DbCatService());
        LocalDate ld = LocalDate.of(2015, 10, 5);
        CreateCatDto createCat = new CreateCatDto("Cat1", ld, "C", "Ginger");
        addCatController.AddCat(createCat);

        AddOwnerController addOwnerController = new AddOwnerController(new DbOwnerService());
        LocalDate ld1 = LocalDate.of(2015, 10, 5);
        CreateOwnerDto createOwner = new CreateOwnerDto("Alex", ld1);
        addOwnerController.AddOwner(createOwner);

        AdoptCatController adoptCatController = new AdoptCatController(new DbOwnerService());
        long catId = 1;
        long ownerId = 2;
        AdoptCatDto adoptCat = new AdoptCatDto(catId, ownerId);
        adoptCatController.AdoptCat(adoptCat);

        MakeCatsFriendsController m = new MakeCatsFriendsController(new DbCatService());
        MakeCatsFriendsDto makeCatsFriends = new MakeCatsFriendsDto(1, 2);
        m.MakeCatsFriends(makeCatsFriends);

        RemoveCatController removeCatController = new RemoveCatController(new DbCatService());
        RemoveCatDto removeCat = new RemoveCatDto(1);
        removeCatController.RemoveCat(removeCat);

        RemoveOwnerController removeOwnerController = new RemoveOwnerController(new DbOwnerService());
        RemoveOwnerDto removeOwner = new RemoveOwnerDto(2);
        removeOwnerController.RemoveOwner(removeOwner);*/
    }
}